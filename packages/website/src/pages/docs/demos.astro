---
import Layout from '../../layouts/Layout.astro';
import Section from '../../components/Section.astro';
import SectionHeader from '../../components/SectionHeader.astro';
import Panel from '../../components/Panel.astro';
import Chip from '../../components/Chip.astro';
import DocsWayfinding from '../../components/DocsWayfinding.astro';

const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
---

<Layout title="Demos - Motus">
  <Section tone="dark" pad="hero" class="text-center" innerClass="max-w-3xl">
    <SectionHeader
      eyebrow="Demos"
      title="See the verified loop in action"
      subtitle="These examples track the current release. Full scripts live in GitHub."
      size="xl"
    />
    <div class="mt-phi-3 flex flex-wrap justify-center gap-phi-2 text-sm">
      <a href={`${base}get-started`} class="text-mint hover:text-text-primary transition-colors">
        Get Started &#8594;
      </a>
      <a href={`${base}how-it-works`} class="text-mint hover:text-text-primary transition-colors">
        How it works &#8594;
      </a>
    </div>
  </Section>

  <Section tone="light" pad="tight" innerClass="max-w-5xl">
    <SectionHeader
      eyebrow="Current"
      title="Run the demos"
      subtitle="Short summaries here. Full scripts and outputs live in GitHub."
      align="left"
      size="md"
      class="mb-phi-4"
      maxWidth="max-w-3xl"
    />
    <div class="grid md:grid-cols-2 gap-phi-3">
      <Panel size="sm" class="space-y-phi-2">
        <div class="flex flex-wrap items-center gap-phi-2">
          <Chip>Current</Chip>
          <span class="text-xs uppercase tracking-[0.2em] text-text-secondary">CLI</span>
        </div>
        <h3 class="text-lg font-display text-text-primary">Six-call loop</h3>
        <p class="text-sm text-text-secondary">
          Claim work, attach evidence, and release a receipt in one deterministic loop.
        </p>
        <a
          href="https://github.com/motus-os/motus/blob/main/packages/cli/docs/cli-reference.md"
          class="text-sm text-mint hover:text-text-primary transition-colors"
          target="_blank"
          rel="noopener"
        >
          CLI reference &#8594;
        </a>
      </Panel>
      <Panel size="sm" class="space-y-phi-2">
        <div class="flex flex-wrap items-center gap-phi-2">
          <Chip>Current</Chip>
          <span class="text-xs uppercase tracking-[0.2em] text-text-secondary">Coordination</span>
        </div>
        <h3 class="text-lg font-display text-text-primary">Lease conflict prevention</h3>
        <p class="text-sm text-text-secondary">
          Two agents can’t claim the same task. Leases enforce exclusivity.
        </p>
        <a
          href="https://github.com/motus-os/motus/blob/main/packages/cli/docs/cli-reference.md"
          class="text-sm text-mint hover:text-text-primary transition-colors"
          target="_blank"
          rel="noopener"
        >
          CLI reference &#8594;
        </a>
      </Panel>
      <Panel size="sm" class="space-y-phi-2">
        <div class="flex flex-wrap items-center gap-phi-2">
          <Chip>Current</Chip>
          <span class="text-xs uppercase tracking-[0.2em] text-text-secondary">Proof</span>
        </div>
        <h3 class="text-lg font-display text-text-primary">Workflow compression logs</h3>
        <p class="text-sm text-text-secondary">
          Side-by-side logs showing manual vs receipt handoffs.
        </p>
        <a
          href="https://github.com/motus-os/motus/tree/main/docs/proof/token-reduction"
          class="text-sm text-mint hover:text-text-primary transition-colors"
          target="_blank"
          rel="noopener"
        >
          View logs &#8594;
        </a>
      </Panel>
      <Panel size="sm" class="space-y-phi-2">
        <div class="flex flex-wrap items-center gap-phi-2">
          <Chip>Current</Chip>
          <span class="text-xs uppercase tracking-[0.2em] text-text-secondary">Hands-on</span>
        </div>
        <h3 class="text-lg font-display text-text-primary">Guided walkthrough</h3>
        <p class="text-sm text-text-secondary">
          Build a workflow runner and ship three receipts end to end.
        </p>
        <a href={`${base}get-started`} class="text-sm text-mint hover:text-text-primary transition-colors">
          Get Started &#8594;
        </a>
      </Panel>
    </div>
  </Section>

  <Section tone="light" pad="tight" innerClass="max-w-5xl">
    <SectionHeader
      eyebrow="Future"
      title="Strict enforcement demos"
      subtitle="These scenarios require gates that are not enforced yet."
      align="left"
      size="md"
      class="mb-phi-4"
      maxWidth="max-w-3xl"
    />
    <Panel size="sm" class="space-y-phi-2">
      <div class="flex flex-wrap items-center gap-phi-2">
        <Chip>Future</Chip>
        <span class="text-xs uppercase tracking-[0.2em] text-text-secondary">Planned</span>
      </div>
      <ul class="text-sm text-text-secondary space-y-phi-1">
        <li>Fail-closed release when evidence or decision is missing.</li>
        <li>Receipt hash verification (proof-of-work audit).</li>
        <li>Gate configuration from policy files.</li>
        <li>Context transfer between agents on handoff.</li>
      </ul>
      <p class="text-sm text-text-secondary">
        These require enforcement gates. Track progress in the ecosystem map and evidence registry.
      </p>
      <div class="flex flex-wrap gap-phi-2 text-sm">
        <a href={`${base}docs/ecosystem`} class="text-mint hover:text-text-primary transition-colors">
          Ecosystem map &#8594;
        </a>
        <a href={`${base}docs/evidence`} class="text-mint hover:text-text-primary transition-colors">
          Evidence registry &#8594;
        </a>
      </div>
    </Panel>
  </Section>

  <Section tone="light" pad="tight" innerClass="max-w-5xl">
    <DocsWayfinding pageId="demos" base={base} />
  </Section>
</Layout>
